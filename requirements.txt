# Network Security Q&A Chatbot - Requirements
# Install using: pip install -r requirements.txt
# Note: For Python 3.14, install pydantic first: pip install "pydantic>=2.12.0"
# Then install qdrant-client with --no-deps and install its dependencies separately if needed

# Core dependencies
pydantic>=2.12.0,<3.0.0

# Web framework (FastAPI instead of Gradio)
fastapi>=0.115.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.6

# Core application packages
chromadb>=0.4.0
requests>=2.31.0
sentence-transformers>=2.2.0
pdfplumber>=0.9.0
openai>=1.0.0
gpt4all>=2.5.0
